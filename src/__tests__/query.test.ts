import { describe, it, expect } from "bun:test";

describe("query", () => {
  describe("query", () => {
    it.todo("should call /query?q= with URL-encoded SOQL");
    it.todo("should return totalSize, done, and records");
  });

  describe("query_all", () => {
    it.todo("should call /queryAll?q= with URL-encoded SOQL");
    it.todo("should include soft-deleted records");
  });
});
